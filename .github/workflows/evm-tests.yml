name: EVM Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  blockchain-test-0-12000:
    name: BlockchainTest [0-12000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=0 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t BlockchainTest

  blockchain-test-12000-24000:
    name: BlockchainTest [12000-24000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=12000 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t BlockchainTest

  blockchain-test-24000-36000:
    name: BlockchainTest [24000-36000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=24000 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t BlockchainTest

  blockchain-test-36000-48000:
    name: BlockchainTest [36000-48000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=36000 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t BlockchainTest

  state-test-0-12000:
    name: StateTest [0-12000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=0 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t StateTest

  state-test-12000-24000:
    name: StateTest [12000-24000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=12000 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t StateTest

  state-test-24000-36000:
    name: StateTest [24000-36000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=24000 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t StateTest

  state-test-36000-48000:
    name: StateTest [36000-48000]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci-setup
      - uses: astral-sh/setup-uv@v4
      - name: Download test fixtures
        working-directory: packages/evm
        run: uvx --from "git+https://github.com/ethereum/execution-specs#subdirectory=packages/testing" consume cache --input=stable@latest --extract-to ./test-fixtures
      - run: pnpm run build
      - run: LIMIT=12000 SKIP=36000 pnpm --filter @evm-effect/evm exec vitest run test/fixtures.test.ts -t StateTest
