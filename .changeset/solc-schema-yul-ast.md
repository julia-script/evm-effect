---
"@evm-effect/solc": patch
---

Refine Solidity and Yul output schemas: use `YulBlock` for mixed source AST, model contract IR as `YulObjectSchema`, re-export AST modules from `output`, and extend schema tests.
