---
"@evm-effect/solc": minor
---

Add Effect schemas for standard compiler JSON AST (`SolcAst` / `AstNodeSchema`) and type `SourceOutput.ast` instead of `unknown`. Include solc-backed tests that decode real compile output.
