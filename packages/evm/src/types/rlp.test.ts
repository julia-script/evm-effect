import { describe, it } from "vitest";

describe("Rlp", () => {
  it("should encode a block", () => {
    // console.log("test");
    // const block = Block.create({});
    // const encoded = yield* Rlp.encode(block);
    // expect(encoded).toBe(block.toRlp());
  });
});
