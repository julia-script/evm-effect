{
  "name": "@evm-effect/examples",
  "version": "0.0.22",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "dev": "tsc -b tsconfig.build.json --watch",
    "check": "tsc -b tsconfig.json",
    "hello-world": "tsx src/hello-world.ts",
    "blockchain-transaction": "tsx src/blockchain-transaction.ts",
    "deploy-contract": "tsx src/deploy-contract.ts"
  },
  "dependencies": {
    "@evm-effect/crypto": "workspace:*",
    "@evm-effect/ethereum-types": "workspace:*",
    "@evm-effect/evm": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "tsx": "^4.21.0",
    "typescript": "catalog:"
  }
}
